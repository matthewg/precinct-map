join_keys:
  geo_join_key: PRECNAME
  csv_join_key: precinct
display:
  title: "Voter Turnout"
  type: numeric_closed
  value_column: turnout
  numeric_bounds:
    min: 0
    max: 100
  numeric_colors:
    min_color: "#ffcccc"
    max_color: "#cc0000"
  drop_unmatched_geo: true
  tooltips:
    - PRECNAME
    - turnout
